apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: $CI_COMMIT_REF_NAME-ingress-wildcard
  namespace: $CI_COMMIT_REF_NAME
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.org/server-snippets: |
      gzip on;
      gzip_http_version 1.1;
      gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
      listen 443 ssl;
      ssl_certificate /etc/nginx/secrets/default;
      ssl_certificate_key /etc/nginx/secrets/default;
      ssl_ciphers NULL;
      if ($scheme = http) {
       return 301 https://$server_name$request_uri;
      }
      server_name *.$CI_COMMIT_REF_NAME.example.com;
spec:
  # tls:
  # - hosts:
  #   - "rick.example.com"
  rules:
  - host: "$CI_COMMIT_REF_NAME.example.com"
    http:
      paths:
      - path: /
        backend:
          serviceName: gotest-app-$CI_COMMIT_REF_NAME
          servicePort: 80
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: $CI_COMMIT_REF_NAME-ingress-nginx
  namespace: $CI_COMMIT_REF_NAME
  annotations:
    kubernetes.io/ingress.class: nginx
    #nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    #nginx.ingress.kubernetes.io/ssl-redirect: "true"
    #service.beta.kubernetes.io/do-loadbalancer-redirect-http-to-https: "true"
    nginx.org/server-snippets: |
      gzip on;
      gzip_http_version 1.1;
      gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
      listen 443 ssl;
      ssl_certificate /etc/nginx/secrets/default;
      ssl_certificate_key /etc/nginx/secrets/default;
      ssl_ciphers NULL;
      if ($scheme = http) {
       return 301 https://$server_name$request_uri;
      }
spec:
  # tls:
  # - hosts:
  #   - "nginx.rick.example.com"
  rules:
  - host: "nginx.$CI_COMMIT_REF_NAME.example.com"
    http:
      paths:
      - path: /
        backend:
          serviceName: nginx-2-srv
          servicePort: 80
